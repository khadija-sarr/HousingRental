<a class="cards" href="{{ path('default_house', {
    'category': house.category.alias,
    'alias': house.alias,
    'id':house.id
}) }}">
    <img src="{{ asset('uploads/houses/' ~ house.photo) }}" alt="Card image">
    <div class="filter">
        <div class="cardBody" >
            <h5 class="">{{ house.name }}</h5>
            <h6 class="">{{ house.city }}, {{ house.price }} €/nuit</h6>
        </div>
        <div class="categoryCard">
            <p >{{ house.category.name | title }} </p>
        </div>
    </div>
</a>