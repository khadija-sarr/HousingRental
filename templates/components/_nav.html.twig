<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('default_home') }}">HousingRental</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('default_category', {'alias' : 'appartement'}) }}">Appartement</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('default_category', {'alias' : 'maison'}) }}">Maison</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('default_category', {'alias' : 'evenement'}) }}">Évenement</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_backOffice') }}">Back Office</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('house_new') }}">Logement</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('user_register') }}">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
<div class="navHeight"></div>